name: country_codes
description: Plugin for working with ISO Country and Dial codes.
version: 1.0.0
author: Yakup Murat ÇAYLAK
homepage: https://github.com/RVYA/country_codes

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  plugin:
    platforms:
      android:
        package: com.ymc.country_codes
        pluginClass: CountryCodesPlugin
      ios:
        pluginClass: CountryCodesPlugin